# YourAnimeList-Server
----------------------
1. start by adding the node modules
2. npm init
3. npm install express
4. npm install cors
5. start the server, node index.js
----------------------

Written in JS (Node.js and Express.js)
